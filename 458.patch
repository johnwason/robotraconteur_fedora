From 7e3cac2b63d7e43573914d799508a1a1b629200b Mon Sep 17 00:00:00 2001
From: John Wason <wason@wasontech.com>
Date: Sat, 4 Jul 2026 00:06:28 -0400
Subject: [PATCH 1/3] Fix soft load so library suffix in hardware transport
 using dlopen

---
 .../src/HardwareTransport_libusb.cpp             |  8 +++++++-
 .../src/HardwareTransport_linux.cpp              | 16 ++++++++++++++--
 2 files changed, 21 insertions(+), 3 deletions(-)

diff --git a/RobotRaconteurCore/src/HardwareTransport_libusb.cpp b/RobotRaconteurCore/src/HardwareTransport_libusb.cpp
index 886b8fe2a..0e8b913a7 100644
--- a/RobotRaconteurCore/src/HardwareTransport_libusb.cpp
+++ b/RobotRaconteurCore/src/HardwareTransport_libusb.cpp
@@ -42,9 +42,15 @@ bool LibUsb_Functions::LoadFunctions()
 {
     if (lib_handle)
         throw InvalidOperationException("libusb functions already loaded");
-    lib_handle = dlopen("libusb-1.0.so", RTLD_LAZY);
+    lib_handle = dlopen("libusb-1.0.so.0", RTLD_LAZY);
+    if (!lib_handle)
+    {
+        lib_handle = dlopen("libusb-1.0.so", RTLD_LAZY);
+    }
     if (!lib_handle)
         return false;
+    else
+        dlerror();
 
     LIBUSB_FUNCTIONS_INIT(LIBUSB_FUNCTIONS_PTR_INIT);
 
diff --git a/RobotRaconteurCore/src/HardwareTransport_linux.cpp b/RobotRaconteurCore/src/HardwareTransport_linux.cpp
index 99e96041b..770d28ba8 100644
--- a/RobotRaconteurCore/src/HardwareTransport_linux.cpp
+++ b/RobotRaconteurCore/src/HardwareTransport_linux.cpp
@@ -43,9 +43,15 @@ bool DBus_Functions::LoadFunctions()
 {
     if (lib_handle)
         throw InvalidOperationException("dbus functions already loaded");
-    lib_handle = dlopen("libdbus-1.so", RTLD_LAZY);
+    lib_handle = dlopen("libdbus-1.so.3", RTLD_LAZY);
+    if (!lib_handle)
+    {
+        lib_handle = dlopen("libdbus-1.so", RTLD_LAZY);
+    }
     if (!lib_handle)
         return false;
+    else
+        dlerror();
 
     DBUS_FUNCTIONS_INIT(DBUS_FUNCTIONS_PTR_INIT);
 
@@ -73,9 +79,15 @@ bool Sdp_Functions::LoadFunctions()
 {
     if (lib_handle)
         throw InvalidOperationException("sdp functions already loaded");
-    lib_handle = dlopen("libbluetooth.so", RTLD_LAZY);
+    lib_handle = dlopen("libbluetooth.so.3", RTLD_LAZY);
+    if (!lib_handle)
+    {
+        lib_handle = dlopen("libbluetooth.so", RTLD_LAZY);
+    }
     if (!lib_handle)
         return false;
+    else
+        dlerror();
 
     SDP_FUNCTIONS_INIT(SDP_FUNCTIONS_PTR_INIT);
 

From 0ee30d4b9ba789c2184c74cf6bd9e5156eb80d8b Mon Sep 17 00:00:00 2001
From: John Wason <wason@wasontech.com>
Date: Sat, 4 Jul 2026 01:36:29 -0400
Subject: [PATCH 2/3] Fix memory protection error in GetDetectedDevicesPaths()

---
 RobotRaconteurCore/src/HardwareTransport_libusb.cpp | 4 +++-
 1 file changed, 3 insertions(+), 1 deletion(-)

diff --git a/RobotRaconteurCore/src/HardwareTransport_libusb.cpp b/RobotRaconteurCore/src/HardwareTransport_libusb.cpp
index 0e8b913a7..cd15e23ca 100644
--- a/RobotRaconteurCore/src/HardwareTransport_libusb.cpp
+++ b/RobotRaconteurCore/src/HardwareTransport_libusb.cpp
@@ -409,6 +409,7 @@ std::list<UsbDeviceManager_detected_device> LibUsbDeviceManager::GetDetectedDevi
                 int f_bos = open(bos_path.c_str(), O_RDONLY);
                 if (f_bos >= 0)
                 {
+                    sysfs_bos_desc.clear();
                     sysfs_bos_desc.resize(UINT16_MAX);
                     int bos_len = (int)read(f_bos, &sysfs_bos_desc[0], UINT16_MAX);
                     if (bos_len > 0)
@@ -442,7 +443,8 @@ std::list<UsbDeviceManager_detected_device> LibUsbDeviceManager::GetDetectedDevi
                                 {
                                     break;
                                 }
-                                std::vector<uint8_t> desc(&sysfs_bos_desc[p + 4], &sysfs_bos_desc[p + len]);
+                                std::vector<uint8_t> desc(sysfs_bos_desc.begin() + (p + 4),
+                                                          sysfs_bos_desc.begin() + (p + len));
                                 platform_bos_desc.push_back(desc);
                             }
                             p += len;

From 93bcbbbb2e6860f013dce01f212d734841fc53a6 Mon Sep 17 00:00:00 2001
From: John Wason <wason@wasontech.com>
Date: Sun, 5 Jul 2026 17:10:06 -0400
Subject: [PATCH 3/3] Fix clang-tidy warning

---
 RobotRaconteurCore/src/HardwareTransport_libusb.cpp | 3 +--
 1 file changed, 1 insertion(+), 2 deletions(-)

diff --git a/RobotRaconteurCore/src/HardwareTransport_libusb.cpp b/RobotRaconteurCore/src/HardwareTransport_libusb.cpp
index cd15e23ca..eee1f287d 100644
--- a/RobotRaconteurCore/src/HardwareTransport_libusb.cpp
+++ b/RobotRaconteurCore/src/HardwareTransport_libusb.cpp
@@ -443,8 +443,7 @@ std::list<UsbDeviceManager_detected_device> LibUsbDeviceManager::GetDetectedDevi
                                 {
                                     break;
                                 }
-                                std::vector<uint8_t> desc(sysfs_bos_desc.begin() + (p + 4),
-                                                          sysfs_bos_desc.begin() + (p + len));
+                                std::vector<uint8_t> desc(&sysfs_bos_desc[0] + p + 4U, &sysfs_bos_desc[0] + p + len);
                                 platform_bos_desc.push_back(desc);
                             }
                             p += len;
